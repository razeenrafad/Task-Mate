# Procfile for Heroku/Render deployment
# This file tells the deployment platform how to run the application
# The 'web' process type runs the Flask application using Gunicorn WSGI server
web: gunicorn app:app

