# Flask web framework for building the web application
Flask==2.3.3

# SQLAlchemy ORM for database operations
Flask-SQLAlchemy==3.0.5

# Werkzeug WSGI utility library (dependency of Flask)
Werkzeug==2.3.7

# Jinja2 templating engine (dependency of Flask)
Jinja2==3.1.2

# Click command line interface creation toolkit (dependency of Flask)
click==8.1.7

# MarkupSafe secure string handling (dependency of Jinja2)
MarkupSafe==2.1.3

# ItsDangerous secure data serialization (dependency of Flask)
itsdangerous==2.1.2

# SQLite3 database driver (built into Python)
# No additional package needed for SQLite

# Gunicorn WSGI HTTP Server for production deployment
gunicorn==21.2.0

